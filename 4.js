// 4] whether x is multiple of y
const multiple = function (x, y) {
  // const r = ;

  if (x % y === 0) {
    console.log(`${x} is multiple of ${y}`);
  } else {
    console.log(`${x} is not multiple of ${y}`);
  }
};

multiple(26, 13);
